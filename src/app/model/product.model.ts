export interface Product {
  productId: number;
  productName: string;
  productDescription: string;
  category: string;
  brand: string;
  batchNumber: string;
  expireDate: string;
  manufacturedDate: string;
  createdDate: string;
  quantity: number;
  unitPrice: number;
}
